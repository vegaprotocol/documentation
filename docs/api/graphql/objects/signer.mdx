---
id: signer
title: Signer
hide_table_of_contents: false
---


export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={'badge badge--' + props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};




Signer is the authorized signature used for the data.


```graphql
type Signer {
  signer: SignerKind!
}
```


### Fields

#### [<code style={{ fontWeight: 'normal' }}>Signer.<b>signer</b></code>](#)<Bullet />[`SignerKind!`](/testnet/api/graphql/unions/signer-kind) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="union"/>
> 
> 



### Member of

[`Data`](/testnet/api/graphql/objects/data)  <Badge class="secondary" text="object"/><Bullet />[`DataSourceSpecConfiguration`](/testnet/api/graphql/objects/data-source-spec-configuration)  <Badge class="secondary" text="object"/>

