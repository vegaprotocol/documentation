---
id: new-market-commitment-input
title: NewMarketCommitmentInput
---

A commitment of liquidity to be made by the party which proposes a market

```graphql
type NewMarketCommitmentInput {
  commitmentAmount: String!
  fee: String!
  sells: [LiquidityOrderInput!]
  buys: [LiquidityOrderInput!]
  reference: String
}
```

### Fields

#### `commitmentAmount` ([`String!`](/docs/graphql/scalars/string))

Specified as a unitless number that represents the amount of settlement asset of the market

#### `fee` ([`String!`](/docs/graphql/scalars/string))

Nominated liquidity fee factor, which is an input to the calculation of
taker fees on the market, as per setting fees and rewarding liquidity provider

#### `sells` ([`[LiquidityOrderInput!]`](/docs/graphql/inputs/liquidity-order-input))

A set of liquidity sell orders to meet the liquidity provision obligation

#### `buys` ([`[LiquidityOrderInput!]`](/docs/graphql/inputs/liquidity-order-input))

A set of liquidity buy orders to meet the liquidity provision obligation

#### `reference` ([`String`](/docs/graphql/scalars/string))

A reference to be associated to all orders created from this commitment
