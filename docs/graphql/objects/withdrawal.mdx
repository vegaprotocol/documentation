---
id: withdrawal
title: Withdrawal
---

The details of a withdrawal processed by vega

```graphql
type Withdrawal {
  id: ID!
  party: Party!
  amount: String!
  asset: Asset!
  status: WithdrawalStatus!
  ref: String!
  expiry: String!
  createdTimestamp: String!
  withdrawnTimestamp: String
  txHash: String
  details: WithdrawalDetails
}
```

### Fields

#### `id` ([`ID!`](/docs/graphql/scalars/id))

The Vega internal id of the withdrawal

#### `party` ([`Party!`](/docs/graphql/objects/party))

The PartyID initiating the witndrawal

#### `amount` ([`String!`](/docs/graphql/scalars/string))

The amount to be withdrawn

#### `asset` ([`Asset!`](/docs/graphql/objects/asset))

The asset to be withdrawn

#### `status` ([`WithdrawalStatus!`](/docs/graphql/enums/withdrawal-status))

The current status of the withdrawal

#### `ref` ([`String!`](/docs/graphql/scalars/string))

A reference the foreign chain can use to refere to when processing the withdrawal

#### `expiry` ([`String!`](/docs/graphql/scalars/string))

RFC3339Nano time until the withdrawal will be invalid

#### `createdTimestamp` ([`String!`](/docs/graphql/scalars/string))

RFC3339Nano time at which the withdrawal was created

#### `withdrawnTimestamp` ([`String`](/docs/graphql/scalars/string))

RFC3339Nano time at which the withdrawal was finalized

#### `txHash` ([`String`](/docs/graphql/scalars/string))

Hash of the transaction on the foreign chain

#### `details` ([`WithdrawalDetails`](/docs/graphql/unions/withdrawal-details))

Foreign chain specific details about the withdrawal
