---
id: asset
title: Asset
---

Represents an asset in vega

```graphql
type Asset {
  id: ID!
  name: String!
  symbol: String!
  totalSupply: String!
  decimals: Int!
  source: AssetSource!
  infrastructureFeeAccount: Account!
}
```

### Fields

#### `id` ([`ID!`](/graphql/scalars/id))

The id of the asset

#### `name` ([`String!`](/graphql/scalars/string))

The full name of the asset (e.g: Great British Pound)

#### `symbol` ([`String!`](/graphql/scalars/string))

The symbol of the asset (e.g: GBP)

#### `totalSupply` ([`String!`](/graphql/scalars/string))

The total supply of the market

#### `decimals` ([`Int!`](/graphql/scalars/int))

The precision of the asset

#### `source` ([`AssetSource!`](/graphql/unions/asset-source))

The origin source of the asset (e.g: an erc20 asset)

#### `infrastructureFeeAccount` ([`Account!`](/graphql/objects/account))

The infrastructure fee account for this asset
