---
id: oracle-spec
title: OracleSpec
---

An oracle spec describe the oracle data that a product (or a risk model)
wants to get from the oracle engine.

```graphql
type OracleSpec {
  id: String!
  createdAt: String!
  updatedAt: String
  pubKeys: [String!]
  filters: [Filter!]
  status: OracleSpecStatus!
  data: [OracleData!]
}
```

### Fields

#### `id` ([`String!`](/graphql/scalars/string))

id is a hash generated from the OracleSpec data.

#### `createdAt` ([`String!`](/graphql/scalars/string))

RFC3339Nano creation date time

#### `updatedAt` ([`String`](/graphql/scalars/string))

RFC3339Nano last updated timestamp

#### `pubKeys` ([`[String!]`](/graphql/scalars/string))

pubKeys is the list of authorized public keys that signed the data for this
oracle. All the public keys in the oracle data should be contained in these
public keys.

#### `filters` ([`[Filter!]`](/graphql/objects/filter))

filters describes which oracle data are considered of interest or not for
the product (or the risk model).

#### `status` ([`OracleSpecStatus!`](/graphql/enums/oracle-spec-status))

status describes the status of the oracle spec

#### `data` ([`[OracleData!]`](/graphql/objects/oracle-data))

data list all the oracle data broadcast to this spec
