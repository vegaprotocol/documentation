---
id: asset
title: Asset
---


Represents an asset in vega

```graphql
type Asset {
  id: ID!
  name: String!
  symbol: String!
  totalSupply: String!
  decimals: Int!
  minLpStake: String!
  source: AssetSource!
  infrastructureFeeAccount: Account!
  globalRewardPoolAccount: Account
}
```


### Fields

#### `id` ([`ID`](/docs/v0.47/graphql/scalars/id))

The id of the asset


#### `name` ([`String`](/docs/v0.47/graphql/scalars/string))

The full name of the asset (e.g: Great British Pound)


#### `symbol` ([`String`](/docs/v0.47/graphql/scalars/string))

The symbol of the asset (e.g: GBP)


#### `totalSupply` ([`String`](/docs/v0.47/graphql/scalars/string))

The total supply of the market


#### `decimals` ([`Int`](/docs/v0.47/graphql/scalars/int))

The precision of the asset


#### `minLpStake` ([`String`](/docs/v0.47/graphql/scalars/string))

The min stake to become an lp for any market using this asset for settlement


#### `source` ([`AssetSource`](/docs/v0.47/graphql/unions/asset-source))

The origin source of the asset (e.g: an erc20 asset)


#### `infrastructureFeeAccount` ([`Account`](/docs/v0.47/graphql/objects/account))

The infrastructure fee account for this asset


#### `globalRewardPoolAccount` ([`Account`](/docs/v0.47/graphql/objects/account))

The global reward pool account for this asset




