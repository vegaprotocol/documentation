---
id: bus-events
title: busEvents
---


Subscribe to event data from the event bus

```graphql
busEvents(
  types: [BusEventType!]!
  marketId: ID
  partyId: ID
  batchSize: Int!
): [BusEvent!]
```


### Arguments

#### `types` ([`BusEventType`](/docs/mainnet/graphql/enums/bus-event-type))

the types to subscribe to has to be an array


#### `marketId` ([`ID`](/docs/mainnet/graphql/scalars/id))

optional filter by market ID


#### `partyId` ([`ID`](/docs/mainnet/graphql/scalars/id))

optional filter by party ID


#### `batchSize` ([`Int`](/docs/mainnet/graphql/scalars/int))

Specifies the size that the client will receive events in. Using 0 results in a variable batch size being sent. The stream will be closed if the client fails to read a batch within 5 seconds


### Type

#### [`BusEvent`](/docs/mainnet/graphql/objects/bus-event) 






